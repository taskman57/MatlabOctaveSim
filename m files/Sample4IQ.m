function Hd = Sample4IQ
%SAMPLE4IQ Returns a discrete-time filter object.

% MATLAB Code
% Generated by MATLAB(R) 8.3 and the DSP System Toolbox 8.6.
% Generated on: 09-Jun-2017 04:23:23

% Chebyshev Type II Lowpass filter designed using FDESIGN.LOWPASS.

% All frequency values are in MHz.
Fs = 200;  % Sampling Frequency

Fpass = 0.01;        % Passband Frequency
Fstop = 0.05;        % Stopband Frequency
Apass = 1;           % Passband Ripple (dB)
Astop = 100;         % Stopband Attenuation (dB)
match = 'stopband';  % Band to match exactly

% Construct an FDESIGN object and call its CHEBY2 method.
h  = fdesign.lowpass(Fpass, Fstop, Apass, Astop, Fs);
Hd = design(h, 'cheby2', 'MatchExactly', match);
% Set the arithmetic property.
set(Hd, 'Arithmetic', 'fixed', ...
    'CoeffWordLength', 25, ...
    'CoeffAutoScale', true, ...
    'ProductMode', 'Fullprecision', ...
    'AccumMode', 'KeepMSB', ...
    'AccumWordLength', 40, ...
    'StateWordLength', 16, ...
    'StateFracLength', 15, ...
    'SectioninputWordLength', 16, ...
    'SectioninputAutoScale', true, ...
    'SectionoutputWordLength', 16, ...
    'SectionoutputAutoScale', true, ...
    'InputWordLength', 16, ...
    'inputFracLength', 15, ...
    'OutputWordLength', 16, ...
    'OutputMode', 'AvoidOverflow', ...
    'signed',        true, ...
    'RoundMode',     'convergent', ...
    'OverflowMode',  'Wrap', ...
    'CastBeforeSum', true);



% [EOF]
